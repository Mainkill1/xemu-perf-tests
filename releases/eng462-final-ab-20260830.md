# ENG462 final A/B — 2026-08-30

Verdict: **not release-validated**.

Same dedicated Windows rig, XISO, 1x scale, three warmups, 4x work, and per-iteration completion.

| Backend | Baseline | Candidate | Hash result | Performance result |
| --- | --- | --- | --- | --- |
| OpenGL | 141/141 complete | 141/141 complete | 90 match, 51 differ | Wall 2.88% faster; geometric mean 0.67% faster; median flat |
| Vulkan | 37/141, device lost | 37/141, device lost | Partial: 33 match, 4 differ | Invalid: both runs incomplete |

OpenGL's 18.00% weighted guest-time reduction is dominated by `PgraphPatternPolling` (-45.47%). Without that record, weighted time regresses 3.99%. Confirmed useful paths include PFIFO-only PGR2 (-42.06%), long-scene PFIFO (-33.02%), PGR2 lag-spot inline elements (-13.75%), and SSE scalar (-5.62%). Regressions include S3TC fenced streaming (+8.39%), pipeline churn (+24.37%), texture binding reuse (+10.58%), and TinyDraw variants (+2.89% to +7.60%).

Both Vulkan runs asserted on `VK_ERROR_DEVICE_LOST` in `hw/xbox/nv2a/pgraph/vk/command.c:104` immediately after `Bc2NativeEligible`. Candidate changed three earlier S3TC KAT failures to PASS, but `Dxt1SameAddressQueued` still failed.

The runner's displayed oracle PASS was invalid: it ran zero external checks and recorded null provenance. Direct baseline comparison is the controlling result. ENG-2026-463 adds a tool gate so zero-check runs become unverified and hash mismatch returns failure.

Machine-readable detail and exact run directories are in `eng462-final-ab-20260830.json`.
