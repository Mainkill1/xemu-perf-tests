# Test system

## Five-minute model

```text
test implementation
    + catalog descriptor
            |
            v
      catalog.json
            |
host selects stable leaf IDs
            |
            v
   resolved guest plan
            |
            v
       Xbox XISO
            |
            +-- timing samples and statistics
            +-- work/result checksums and KATs
            +-- framebuffer hash
            +-- structural group outcome
            |
            v
 E:\xemu_perf_tests\results.txt
            |
host extracts the byte-exact result and compares A/B runs
```

Workloads own operations and observations. The runner owns selection,
repetition, environment, extraction, comparison, and reporting. A performance
result is usable only when baseline and candidate execute the same fixed work
and both pass correctness.

## Release status

| Release | Status | Contents |
| --- | --- | --- |
| ENG458 | Canonical | Twelve texture lifetime, S3TC/BC, fallback, and dirty-revalidation routes |
| ENG459 | Preview | Stable catalog IDs, explicit resolved plans, exact leaf completion, structural groups |

ENG459 has a clean NXDK build and 66/66 host contracts. Its LAN guest smoke is
not complete because that machine's xemu lacks SDL X11/Wayland support. This is
an infrastructure limitation, not a guest pass. ENG458 remains the runnable
canonical image until ENG459 completes a guest runtime gate.

## Implemented layout

### Catalog

`resources/catalog.json` is generated by `utils/test_catalog.py`. It contains:

- 129 executable leaves;
- five non-measured groups;
- 134 exact legacy result aliases;
- stable ID and revision;
- suite, display name, description, and tags;
- supported target and isolation policy;
- timeout and measurement class;
- declared observations;
- legacy execution mapping and composite selection bit where required.

The catalog ID hashes the canonical descriptor content. A plan built for a
different catalog is rejected. Edit the generator's descriptor source, then
regenerate; never hand-edit generated C++ or JSON.

### Resolved plan

New automation supplies explicit stable leaf IDs under `resolved_plan`.
The guest rejects:

- wrong schema or catalog ID;
- malformed plan ID;
- unknown, duplicate, or group IDs;
- a declared count that differs from unique selected leaves;
- incomplete composite checkpoints;
- mixing resolved-plan selection with legacy filters.

The guest converts selected composite children into the existing stage masks.
This changes selection, not workload behavior. Legacy suite/test filters remain
available during migration.

### Completion accounting

Each emitted leaf is mapped back to its catalog descriptor. Duplicate,
unexpected, or missing leaves fail resolved-plan validation. Composite parents
are written as `kind=group` with `measurement=null`; they no longer inherit the
last child's timing.

## Test inventory

The generated catalog is the exhaustive per-ID list. This table explains the
suite intent and likely affected xemu path.

| Suite | Leaves | Work and primary signal |
| --- | ---: | --- |
| Busy PFIFO | 2 | FIFO saturation and PGRAPH polling; method/lock/fence overhead |
| CPU floating point | 2 | Fixed x87/SSE arithmetic; TCG/helper performance with result checks |
| CPU translation blocks | 3 | Direct loops and indirect dispatch; translation/branch overhead |
| Fill rate | 2 | Solid and textured sustained rendering; raster/texture throughput |
| High vertex count | 4 | Arrays, inline arrays, inline buffers, inline elements; conversion and dispatch |
| Primitive type | 20 | Fixed-function and shader variants of Xbox primitive topologies |
| Tiny draw | 8 | Small submissions and changing state; CPU/API overhead |
| Uniform thrash | 1 | Repeated constant changes; dirty tracking and uniform synchronization |
| Vertex allocation | 9 | Buffer reuse, overlap, aliasing, and allocation lifetime |
| Surface rendering | 26 | Clears, readback, upload/download, aliases, scaling, and synchronization |
| Game-load composite | 52 | Sustained CPU/PFIFO/GPU/streaming, title-derived lag paths, S3TC, and memory pressure |

Important composite families:

- `long_unlocked_scene`: slowly changing CPU, PFIFO, overdraw, streaming, and
  full-system phases.
- `cross_title_hotpath`: PGR2/Morrowind-derived small draws, vertex writes,
  texture/surface reuse, state churn, scaling, S3TC streaming, and wait control.
- `s3tc_sync_factor`: twelve independently selectable DXT1/RGBA/BC2/BC3
  lifetime and fallback routes. Exact KATs are documented in the README and
  `releases/eng458-xiso-release-v1.json`.
- `doax_menu_representative`, `doax_menu_stress`, `pgr2_ai_backup`: isolated
  and combined fixed-work phases for title-shaped contention.
- Vulkan memory pressure: representative and stress variants for allocation,
  reuse, scaling, and retirement behavior.

## What a result proves

| Field | Meaning |
| --- | --- |
| `source_kat` | Generated input bytes match the intended corpus |
| `work_checksum` | The intended operation sequence and fixed work were constructed |
| `result_checksum` | Semantic workload output matches |
| Narrow KAT | A named critical sample, tile, register, or state value matches |
| `framebuffer_fnv1a64` | Canonical guest framebuffer is unchanged for this regression oracle |
| Timing statistics | Duration of the declared guest measurement scope |
| Group outcome | Children completed; it is not a timing sample |

A previous xemu hash is a regression oracle, not proof of retail Xbox behavior.
Hardware goldens must identify console provenance. Missing hashes must not be
self-approved. Vulkan path claims additionally require host counters or
validation evidence; a guest `upload_expectation` only declares the intended
route.

## Run and extract

The current Windows runner command and all ENG458 expected values are in the
README release section. The guest reads configuration from:

```text
E:\xemu_perf_tests\xemu_perf_tests_config.json
D:\xemu_perf_tests_config.json
```

The first existing path wins. Results are written to:

```text
E:\xemu_perf_tests\results.txt
```

The host runner must preserve this file byte-for-byte and record the injected
config, xemu binary identity, XISO identity, backend, scale, completion policy,
logs, validation messages, and normalized view. A missing final record,
unexpected record count, assertion, crash, hang, device loss, new VUID, or
oracle failure invalidates the run.

### Console progress contract

- Before the first result exists, the screen says `RUNNING` and names suite
  initialization or the first test.
- After a test completes, its iterations and total/average/minimum/maximum
  remain visible over the rendered frame.
- A `RUNNING:` footer identifies the active test, suite initialization, or
  suite teardown. The result above it describes completed work.
- The footer is rebuilt from a clean pbkit text buffer. It must not accumulate,
  overlap older lines, clear the rendered frame, or flash a blank screen.
- A soft assertion uses a light-red failure screen for ten seconds; releasing A
  advances early. Execution continues and output retains the failure.

Do not diagnose a hang from the result title alone. Record the final
`RUNNING:` footer and host heartbeat. ENG462 exposed a long pause after
`GameLoadComposite` as `Suite initialize: High vertex count`: saved benchmark
mode was constructing four large continuous-mode geometry sets its tests never
selected. Saved mode now constructs only `single_frame_geometry_`; interactive
continuous mode retains the large sets.

Local run
`C:\xemu-lab\runs\suite\2026-08-30-203104-perf-full-suite-unknown`
completed 141/141 records on OpenGL 1x. Zero warmups and work multiplier one
made this a lifecycle/correctness diagnostic, not performance evidence.

For performance, use the same resolved work for both builds. Short operations
must be batched into seconds-long samples; use warmups and repeated interleaved
A/B processes. Never compare separately calibrated candidate work.

## Source map

| Path | Responsibility |
| --- | --- |
| `src/tests/` | Workload bodies and suite registration |
| `src/test_host.*` | Timing, result emission, framebuffer hashing, leaf completion |
| `src/runtime_config.*` | Legacy configuration and resolved-plan validation |
| `src/test_catalog.*` | Runtime stable-ID lookup |
| `src/generated/test_catalog.inc` | Generated guest catalog table |
| `resources/catalog.json` | Generated machine catalog |
| `resources/plans/` | Checked plan examples |
| `tests/` | Host contract tests for catalog and workload invariants |
| `releases/` | Versioned image/workload contracts |
| `utils/test_catalog.py` | Catalog generator and validation source |

See [adding-tests.md](adding-tests.md) before extending a suite.
