# Test failure guide

Generated from catalog `sha256:c3471cd082839b28452d344f52552a988831697779482f652a6e1157f16b1d45`. It covers all 141 entries; do not edit this file directly.

## Reading a failure

- `outcome: FAIL` means a guest known-answer, invariant, or semantic check failed. A soft failure continues the suite but still fails the run.
- A framebuffer mismatch with passing internal checks usually points to rendering, readback, undefined pixels, or an unapproved oracle—not automatically to the measured hot path.
- A passing result that is slower is a performance regression, not a functional failure. Compare identical work, completion mode, backend, scale, and warm state.
- If upstream and candidate fail identically, investigate the test/oracle or a shared upstream defect before blaming the patch.
- Group entries summarize children. Diagnose the first failing child; groups intentionally have no timing measurement.
- Never replace a golden because one build disagrees. Retail-hardware evidence or a proven specification establishes correctness.

## `busy_pfifo`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `busy_pfifo.pfifo_saturation` | leaf | PFIFO did not consume the fixed push-buffer stream or produced the wrong final image. Check DMA get/put handling, packet decode, FIFO stalls, and PFIFO/PGRAPH lock handoff. |
| `busy_pfifo.pgraph_pattern_polling` | leaf | The guest-observed PGRAPH completion pattern or rendered result changed. Check report/status polling, notification ordering, GPU completion waits, and busy-bit transitions. |

## `pfifo_array_elements`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `pfifo_array_elements.array_element16` | leaf | The 16-bit element stream produced wrong indices, vertices, or pixels. Check non-incrementing method packet length, endian unpacking, index expansion, bounds growth, and bulk PFIFO dispatch. |
| `pfifo_array_elements.array_element32` | leaf | The 32-bit element stream produced wrong indices, vertices, or pixels. Check non-incrementing method packet length, endian unpacking, index expansion, bounds growth, and bulk PFIFO dispatch. |
| `pfifo_array_elements.array_element_pgr2` | leaf | The pgr2-shaped mixed packet stream produced wrong indices, vertices, or pixels. Check non-incrementing method packet length, endian unpacking, index expansion, bounds growth, and bulk PFIFO dispatch. |

## `cpu_floating_point`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `cpu_floating_point.sse_scalar` | leaf | SSE scalar values or flags differ from the known-answer sequence. Check TCG SSE helpers, MXCSR rounding/FTZ/DAZ state, NaN handling, and native hard-FPU shortcuts. |
| `cpu_floating_point.x87_scalar` | leaf | x87 values, classifications, or status differ. Check extended precision, control-word rounding, exceptions, NaN/denormal handling, and hard-FPU conversion boundaries. |

## `cpu_translation_blocks`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `cpu_translation_blocks.direct_loop` | leaf | A direct-branch loop produced the wrong checksum or stopped making progress. Check TCG block chaining, instruction counting, interrupt exits, and translated-code invalidation. |
| `cpu_translation_blocks.indirect_dispatch` | leaf | The fixed indirect jump-table sequence diverged. Check indirect TB lookup, target caching, register state at exits, and branch dispatch. |
| `cpu_translation_blocks.indirect_dispatch_stress` | leaf | The larger indirect-target set diverged or timed out. Check TB hash/lookup behavior, eviction, chaining misses, and state restoration across repeated indirect exits. |

## `fill_rate`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `fill_rate.solid` | leaf | Solid fragments have wrong coverage/color or the draw does not complete. Check clear/raster state, color masks, viewport/scissor, render-pass setup, and host fill path. |
| `fill_rate.textured` | leaf | Textured fragments differ or texture sampling stalls. Check texture upload/layout, sampler state, coordinates, cache invalidation, and fragment-shader translation. |

## `pipeline_texture_switch`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `pipeline_texture_switch.texture_switch` | leaf | Switching texture bindings preserved stale image/sampler state or rebuilt the wrong pipeline. Check descriptor keys, texture generations, binding dirty flags, and pipeline reuse. |
| `pipeline_texture_switch.shader_negative_control` | leaf | A real shader-state change was incorrectly suppressed. Check shader/pipeline key normalization and dirty-bit elision; this control must change output when shader state changes. |
| `pipeline_texture_switch.clear_texture_normal` | leaf | State leaked across clear, texture-only, and normal-draw boundaries. Check clear-pipeline lifetime, descriptor dirtiness, render-pass transitions, and post-clear state restoration. |
| `pipeline_texture_switch.sampler_only_identity` | leaf | An identical sampler write changed output or caused an invalid reuse decision. Check sampler identity keys, compare-before-dirty logic, descriptor cache lifetime, and texture-stage normalization. |

## `game_load`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `game_load.long_unlocked_scene` | group | At least one child checkpoint failed, a child result is missing, or group completion was recorded incorrectly. Inspect the child records in order; the group has no independent timing oracle. |
| `game_load.long_unlocked_scene.cpu` | leaf | The sustained cpu phase failed or stopped advancing. Check TCG execution, branches, MMIO callbacks, and guest timing. |
| `game_load.long_unlocked_scene.pfifo` | leaf | The sustained pfifo phase failed or stopped advancing. Check push-buffer decode, method dispatch, FIFO stalls, and PFIFO/PGRAPH locks. |
| `game_load.long_unlocked_scene.alpha_overdraw` | leaf | The sustained alpha overdraw phase failed or stopped advancing. Check blend state, depth ordering, fragment coverage, and fill/host-GPU execution. |
| `game_load.long_unlocked_scene.streaming_surface_reuse` | leaf | The sustained streaming surface reuse phase failed or stopped advancing. Check surface/texture generations, uploads, alias ownership, and cache reuse. |
| `game_load.long_unlocked_scene.combined` | leaf | The sustained combined phase failed or stopped advancing. Check CPU-to-PFIFO overlap, renderer synchronization, and critical-path lock waits. |
| `game_load.long_unlocked_scene.full_system` | leaf | The sustained full system phase failed or stopped advancing. Check combined CPU, PFIFO, renderer, streaming, completion, and lifecycle interactions. |
| `game_load.cross_title_hotpath` | group | At least one child checkpoint failed, a child result is missing, or group completion was recorded incorrectly. Inspect the child records in order; the group has no independent timing oracle. |
| `game_load.cross_title_hotpath.queued_vertex_cpu_writes` | leaf | The queued vertex cpu writes stage diverged. Check CPU-written vertex generations, dirty ranges, queued uploads, and host-buffer lifetime. |
| `game_load.cross_title_hotpath.pgr2_small_draws` | leaf | The pgr2 small draws stage diverged. Check tiny-draw dispatch, redundant state suppression, vertex allocation, and pipeline binding. |
| `game_load.cross_title_hotpath.texture_update_reuse` | leaf | The texture update reuse stage diverged. Check texture dirty tracking, same-address replacement, cache keys, and upload reuse. |
| `game_load.cross_title_hotpath.surface_reuse` | leaf | The surface reuse stage diverged. Check surface compatibility keys, attachment ownership, dirty transitions, and fence-safe reuse. |
| `game_load.cross_title_hotpath.pipeline_state_churn` | leaf | The pipeline state churn stage diverged. Check shader/pipeline key normalization, cache invalidation, and redundant bind suppression. |
| `game_load.cross_title_hotpath.blend_constant_reuse` | leaf | The blend constant reuse stage diverged. Check blend-constant comparison, dynamic-state dirtiness, and command emission. |
| `game_load.cross_title_hotpath.texture_binding_reuse` | leaf | The texture binding reuse stage diverged. Check descriptor identity, texture-stage dirtiness, and stale binding reuse. |
| `game_load.cross_title_hotpath.pgr2_lagspot_inline_elements` | leaf | The pgr2 lagspot inline elements stage diverged. Check inline element packet decode, bulk method handling, index expansion, and small-draw submission. |
| `game_load.cross_title_hotpath.scaled_surface_pressure` | leaf | The scaled surface pressure stage diverged. Check scaled extent math, surface copies, aliasing, attachment cache pressure, and readback. |
| `game_load.cross_title_hotpath.s3tc_streaming_fenced_draws` | leaf | The s3tc streaming fenced draws stage diverged. Check compressed upload/fallback selection, staging lifetime, fences, and repeated streaming generations. |
| `game_load.cross_title_hotpath.gpu_wait_control` | leaf | The gpu wait control stage diverged. Check finish reason, report/fence completion, queue submission, and CPU wait accounting. |
| `game_load.s3tc_sync_factor` | group | At least one child checkpoint failed, a child result is missing, or group completion was recorded incorrectly. Inspect the child records in order; the group has no independent timing oracle. |
| `game_load.s3tc_sync_factor.dxt1_same_address_wait` | leaf | The DXT1 same-address rewrite followed by an explicit GPU wait cell produced the wrong KAT/frame. Check dirty-generation detection, synchronized replacement, upload bytes, and fence completion. |
| `game_load.s3tc_sync_factor.dxt1_same_address_queued` | leaf | The DXT1 same-address rewrite while work remains queued cell produced the wrong KAT/frame. Check queued resource generations, staging lifetime, write-after-read hazards, and submission ordering. |
| `game_load.s3tc_sync_factor.dxt1_ring_payload_generations` | leaf | The DXT1 rotating payload generations in a fixed address ring cell produced the wrong KAT/frame. Check generation keys, ring wrap, stale descriptor reuse, and in-flight allocation protection. |
| `game_load.s3tc_sync_factor.dxt1_dirty_once_redraw` | leaf | The DXT1 one dirty upload followed by unchanged redraws cell produced the wrong KAT/frame. Check dirty-bit clearing, unchanged-upload elision, hash/cache reuse, and retained texture validity. |
| `game_load.s3tc_sync_factor.rgba8_same_address_wait` | leaf | The RGBA8 same-address rewrite followed by an explicit GPU wait cell produced the wrong KAT/frame. Check dirty-generation detection, synchronized replacement, upload bytes, and fence completion. |
| `game_load.s3tc_sync_factor.rgba8_same_address_queued` | leaf | The RGBA8 same-address rewrite while work remains queued cell produced the wrong KAT/frame. Check queued resource generations, staging lifetime, write-after-read hazards, and submission ordering. |
| `game_load.s3tc_sync_factor.rgba8_ring_payload_generations` | leaf | The RGBA8 rotating payload generations in a fixed address ring cell produced the wrong KAT/frame. Check generation keys, ring wrap, stale descriptor reuse, and in-flight allocation protection. |
| `game_load.s3tc_sync_factor.rgba8_dirty_once_redraw` | leaf | The RGBA8 one dirty upload followed by unchanged redraws cell produced the wrong KAT/frame. Check dirty-bit clearing, unchanged-upload elision, hash/cache reuse, and retained texture validity. |
| `game_load.s3tc_sync_factor.bc2_native_eligible` | leaf | The BC2 a block-compressed texture eligible for native host upload cell produced the wrong KAT/frame. Check compressed-format capability, mip/block dimensions, native upload selection, and sampled texel decoding. |
| `game_load.s3tc_sync_factor.bc2_bordered_fallback` | leaf | The BC2 a bordered block-compressed texture that must take the fallback path cell produced the wrong KAT/frame. Check native-path rejection, CPU decompression, border fixup, expanded format, and fallback upload lifetime. |
| `game_load.s3tc_sync_factor.bc3_native_eligible` | leaf | The BC3 a block-compressed texture eligible for native host upload cell produced the wrong KAT/frame. Check compressed-format capability, mip/block dimensions, native upload selection, and sampled texel decoding. |
| `game_load.s3tc_sync_factor.bc3_bordered_fallback` | leaf | The BC3 a bordered block-compressed texture that must take the fallback path cell produced the wrong KAT/frame. Check native-path rejection, CPU decompression, border fixup, expanded format, and fallback upload lifetime. |
| `game_load.repeated_display` | leaf | Repeated presentation changed the deterministic scene or stopped advancing. Check flip/vblank ordering, scanout ownership, present waits, and retained renderer state. |
| `game_load.repeated_display_boosted` | leaf | The sustained presentation variant diverged under heavier work. Check the same flip path plus queue growth, throttling, resource lifetime, and delayed completion. |
| `game_load.doax_menu_representative.cpu_only` | leaf | The doax menu representative cpu only phase changed its deterministic result. Check TCG/TB execution, branches, MMIO, and guest timer behavior. |
| `game_load.doax_menu_representative.pfifo_only` | leaf | The doax menu representative pfifo only phase changed its deterministic result. Check push-buffer parsing, method dispatch, FIFO progress, and PFIFO/PGRAPH locking. |
| `game_load.doax_menu_representative.gpu_only` | leaf | The doax menu representative gpu only phase changed its deterministic result. Check draw state, shader/pipeline translation, attachments, and host GPU completion. |
| `game_load.doax_menu_representative.streaming_only` | leaf | The doax menu representative streaming only phase changed its deterministic result. Check dirty ranges, texture/surface uploads, cache reuse, and memory ownership. |
| `game_load.doax_menu_representative.cpu_pfifo_gpu` | leaf | The doax menu representative cpu pfifo gpu phase changed its deterministic result. Check CPU/PFIFO/renderer scheduling, synchronization, and critical-path lock handoffs. |
| `game_load.doax_menu_representative.cpu_pfifo_gpu_streaming` | leaf | The doax menu representative cpu pfifo gpu streaming phase changed its deterministic result. Check combined scheduling plus resource generations, transfers, and alias hazards. |
| `game_load.doax_menu_representative.full_system` | leaf | The doax menu representative full system phase changed its deterministic result. Check all prior domains plus lifecycle, pacing, and accumulated state between phases. |
| `game_load.doax_menu_stress.cpu_only` | leaf | The doax menu stress cpu only phase changed its deterministic result. Check TCG/TB execution, branches, MMIO, and guest timer behavior. |
| `game_load.doax_menu_stress.pfifo_only` | leaf | The doax menu stress pfifo only phase changed its deterministic result. Check push-buffer parsing, method dispatch, FIFO progress, and PFIFO/PGRAPH locking. |
| `game_load.doax_menu_stress.gpu_only` | leaf | The doax menu stress gpu only phase changed its deterministic result. Check draw state, shader/pipeline translation, attachments, and host GPU completion. |
| `game_load.doax_menu_stress.streaming_only` | leaf | The doax menu stress streaming only phase changed its deterministic result. Check dirty ranges, texture/surface uploads, cache reuse, and memory ownership. |
| `game_load.doax_menu_stress.cpu_pfifo_gpu` | leaf | The doax menu stress cpu pfifo gpu phase changed its deterministic result. Check CPU/PFIFO/renderer scheduling, synchronization, and critical-path lock handoffs. |
| `game_load.doax_menu_stress.cpu_pfifo_gpu_streaming` | leaf | The doax menu stress cpu pfifo gpu streaming phase changed its deterministic result. Check combined scheduling plus resource generations, transfers, and alias hazards. |
| `game_load.doax_menu_stress.full_system` | leaf | The doax menu stress full system phase changed its deterministic result. Check all prior domains plus lifecycle, pacing, and accumulated state between phases. |
| `game_load.pgr2_ai_backup.cpu_only` | leaf | The pgr2 ai backup cpu only phase changed its deterministic result. Check TCG/TB execution, branches, MMIO, and guest timer behavior. |
| `game_load.pgr2_ai_backup.pfifo_only` | leaf | The pgr2 ai backup pfifo only phase changed its deterministic result. Check push-buffer parsing, method dispatch, FIFO progress, and PFIFO/PGRAPH locking. |
| `game_load.pgr2_ai_backup.gpu_only` | leaf | The pgr2 ai backup gpu only phase changed its deterministic result. Check draw state, shader/pipeline translation, attachments, and host GPU completion. |
| `game_load.pgr2_ai_backup.streaming_only` | leaf | The pgr2 ai backup streaming only phase changed its deterministic result. Check dirty ranges, texture/surface uploads, cache reuse, and memory ownership. |
| `game_load.pgr2_ai_backup.cpu_pfifo_gpu` | leaf | The pgr2 ai backup cpu pfifo gpu phase changed its deterministic result. Check CPU/PFIFO/renderer scheduling, synchronization, and critical-path lock handoffs. |
| `game_load.pgr2_ai_backup.cpu_pfifo_gpu_streaming` | leaf | The pgr2 ai backup cpu pfifo gpu streaming phase changed its deterministic result. Check combined scheduling plus resource generations, transfers, and alias hazards. |
| `game_load.pgr2_ai_backup.full_system` | leaf | The pgr2 ai backup full system phase changed its deterministic result. Check all prior domains plus lifecycle, pacing, and accumulated state between phases. |

## `high_vertex_count`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `high_vertex_count.arrays` | leaf | The large arrays submission lost or reordered vertices. Check packet capacity growth, vertex/index conversion, draw splitting, buffer offsets, and allocation rollover. |
| `high_vertex_count.inline_arrays` | leaf | The large inline arrays submission lost or reordered vertices. Check packet capacity growth, vertex/index conversion, draw splitting, buffer offsets, and allocation rollover. |
| `high_vertex_count.inline_buffers` | leaf | The large inline buffers submission lost or reordered vertices. Check packet capacity growth, vertex/index conversion, draw splitting, buffer offsets, and allocation rollover. |
| `high_vertex_count.inline_elements` | leaf | The large inline elements submission lost or reordered vertices. Check packet capacity growth, vertex/index conversion, draw splitting, buffer offsets, and allocation rollover. |

## `primitive_type`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `primitive_type.line_loop.fixed_function` | leaf | Line Loop assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.line_loop.vertex_shader` | leaf | Line Loop assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.line_strip.fixed_function` | leaf | Line Strip assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.line_strip.vertex_shader` | leaf | Line Strip assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.lines.fixed_function` | leaf | Lines assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.lines.vertex_shader` | leaf | Lines assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.points.fixed_function` | leaf | Points assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.points.vertex_shader` | leaf | Points assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.polygon.fixed_function` | leaf | Polygon assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.polygon.vertex_shader` | leaf | Polygon assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.quad_strip.fixed_function` | leaf | Quad Strip assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.quad_strip.vertex_shader` | leaf | Quad Strip assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.quads.fixed_function` | leaf | Quads assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.quads.vertex_shader` | leaf | Quads assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.triangle_fan.fixed_function` | leaf | Triangle Fan assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.triangle_fan.vertex_shader` | leaf | Triangle Fan assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.triangle_strip.fixed_function` | leaf | Triangle Strip assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.triangle_strip.vertex_shader` | leaf | Triangle Strip assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.triangles.fixed_function` | leaf | Triangles assembly or raster coverage differs on the fixed-function path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |
| `primitive_type.triangles.vertex_shader` | leaf | Triangles assembly or raster coverage differs on the vertex-shader path. Check topology conversion, closure/restart vertices, provoking vertex, clipping, attribute fetch, and viewport rules. |

## `surface`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `surface.basic` | leaf | Basic render-to-surface output differs. Check surface format/pitch, attachment creation, layout transitions, viewport/scissor, and download conversion. |
| `surface.cpu_read_after_gpu_write` | leaf | The CPU did not observe the GPU's newest surface bytes. Check GPU-to-RAM synchronization, dirty ownership, fence waits, download range, and cache invalidation. |
| `surface.cpu_read_clean_surface` | leaf | Reading an already synchronized surface changed data or performed the wrong path. Check clean/dirty state, redundant download elision, RAM validity, and surface ownership epochs. |
| `surface.framebuffer_working_set_002` | leaf | The 2-surface framebuffer working set changed output or resource lifetime. Check surface-cache capacity, eviction safety, attachment reuse, dirty ownership, and deferred destruction. |
| `surface.framebuffer_working_set_008` | leaf | The 8-surface framebuffer working set changed output or resource lifetime. Check surface-cache capacity, eviction safety, attachment reuse, dirty ownership, and deferred destruction. |
| `surface.framebuffer_working_set_032` | leaf | The 32-surface framebuffer working set changed output or resource lifetime. Check surface-cache capacity, eviction safety, attachment reuse, dirty ownership, and deferred destruction. |
| `surface.framebuffer_working_set_064` | leaf | The 64-surface framebuffer working set changed output or resource lifetime. Check surface-cache capacity, eviction safety, attachment reuse, dirty ownership, and deferred destruction. |
| `surface.full_clear_elision_guard` | leaf | A full clear was incorrectly skipped or applied to the wrong channels. Check clear coverage proof, color/depth masks, attachment extent, and dirty-state propagation. |
| `surface.overlapping_surface_churn_representative` | leaf | Ordinary overlapping views retained stale data or invalidated the wrong resource. Check overlap lookup, alias ownership, format/pitch reinterpretation, and upload/download ordering. |
| `surface.overlapping_surface_churn_stress` | leaf | Heavy alias churn exposed stale data, unbounded resources, or a lifetime fault. Check overlap indexing, eviction, deferred destruction, fences, and conservative reconciliation. |
| `surface.partial_channel_clear_guard` | leaf | A masked clear was treated as a full clear or lost untouched channels. Check channel masks, clear fast-path eligibility, blending/write masks, and surface preservation. |
| `surface.surface_download_path` | leaf | Downloaded RAM bytes or the post-download frame differ. Check image-to-buffer usage/layout, scaled blit, depth/stencil conversion, mapping/invalidation, pitch, and swizzle. |
| `surface.surface_list_lookup_002` | leaf | Lookup among 2 active surfaces selected or skipped the wrong overlap. Check address-range boundaries, interval/list ordering, pitch/extent calculation, and stale-list removal. |
| `surface.surface_list_lookup_008` | leaf | Lookup among 8 active surfaces selected or skipped the wrong overlap. Check address-range boundaries, interval/list ordering, pitch/extent calculation, and stale-list removal. |
| `surface.surface_list_lookup_032` | leaf | Lookup among 32 active surfaces selected or skipped the wrong overlap. Check address-range boundaries, interval/list ordering, pitch/extent calculation, and stale-list removal. |
| `surface.surface_list_lookup_128` | leaf | Lookup among 128 active surfaces selected or skipped the wrong overlap. Check address-range boundaries, interval/list ordering, pitch/extent calculation, and stale-list removal. |
| `surface.vulkan_memory_pressure.representative` | group | At least one child checkpoint failed, a child result is missing, or group completion was recorded incorrectly. Inspect the child records in order; the group has no independent timing oracle. |
| `surface.vulkan_memory_pressure.representative.growth` | leaf | The growth checkpoint failed: live Vulkan memory did not grow only as the fixed working set was created. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.representative.plateau` | leaf | The plateau checkpoint failed: memory failed to stabilize after the working set became steady. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.representative.alias_resize` | leaf | The alias resize checkpoint failed: resized or aliased views retained stale allocations/data. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.representative.reuse` | leaf | The reuse checkpoint failed: compatible resources were not safely reused. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.representative.idle_retention` | leaf | The idle retention checkpoint failed: idle resources were retained or reclaimed outside the declared policy. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.stress` | group | At least one child checkpoint failed, a child result is missing, or group completion was recorded incorrectly. Inspect the child records in order; the group has no independent timing oracle. |
| `surface.vulkan_memory_pressure.stress.growth` | leaf | The growth checkpoint failed: live Vulkan memory did not grow only as the fixed working set was created. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.stress.plateau` | leaf | The plateau checkpoint failed: memory failed to stabilize after the working set became steady. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.stress.alias_resize` | leaf | The alias resize checkpoint failed: resized or aliased views retained stale allocations/data. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.stress.reuse` | leaf | The reuse checkpoint failed: compatible resources were not safely reused. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |
| `surface.vulkan_memory_pressure.stress.idle_retention` | leaf | The idle retention checkpoint failed: idle resources were retained or reclaimed outside the declared policy. Check allocation counters, cache keys, fence-complete retirement, eviction policy, and host-memory accounting. |

## `tiny_draw`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `tiny_draw.arrays.fixed_function` | leaf | Tiny arrays draws differ or lose state on the fixed-function path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |
| `tiny_draw.arrays.vertex_shader` | leaf | Tiny arrays draws differ or lose state on the vertex-shader path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |
| `tiny_draw.inline_arrays.fixed_function` | leaf | Tiny inline arrays draws differ or lose state on the fixed-function path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |
| `tiny_draw.inline_arrays.vertex_shader` | leaf | Tiny inline arrays draws differ or lose state on the vertex-shader path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |
| `tiny_draw.inline_buffers.fixed_function` | leaf | Tiny inline buffers draws differ or lose state on the fixed-function path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |
| `tiny_draw.inline_buffers.vertex_shader` | leaf | Tiny inline buffers draws differ or lose state on the vertex-shader path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |
| `tiny_draw.inline_elements.fixed_function` | leaf | Tiny inline elements draws differ or lose state on the fixed-function path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |
| `tiny_draw.inline_elements.vertex_shader` | leaf | Tiny inline elements draws differ or lose state on the vertex-shader path. Check per-packet dispatch, small-buffer allocation, state carry-over, draw batching, attribute fetch, and pipeline binding. |

## `uniform_thrash`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `uniform_thrash.uniform_thrash` | leaf | Rapid constant writes produced stale shader inputs or lost a real update. Check compare-before-dirty logic, dirty-row masks, uniform packing, descriptor staging, and shader constant indexing. |

## `vertex_buffer_allocation`

| Stable ID | Kind | Likely failure cause and first checks |
| --- | --- | --- |
| `vertex_buffer_allocation.mixed.arrays` | leaf | The mixed arrays allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.mixed.inline_arrays` | leaf | The mixed inline arrays allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.mixed.inline_buffers` | leaf | The mixed inline buffers allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.mixed.inline_elements` | leaf | The mixed inline elements allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.tiny.arrays` | leaf | The tiny arrays allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.tiny.inline_arrays` | leaf | The tiny inline arrays allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.tiny.inline_buffers` | leaf | The tiny inline buffers allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.tiny.inline_elements` | leaf | The tiny inline elements allocation sequence reused or addressed the wrong vertex data. Check alignment, ring wrap, allocation lifetime, upload offsets, cache invalidation, and fence protection. |
| `vertex_buffer_allocation.disjoint_same_page` | leaf | Two disjoint vertex ranges sharing one guest page interfered. Check byte-range versus page dirty tracking, upload offsets, cache keys, and allocation aliasing. |
