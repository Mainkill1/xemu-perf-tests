diff --git a/hw/xbox/nv2a/pgraph/vk/draw.c b/hw/xbox/nv2a/pgraph/vk/draw.c
index 4cde8ea001..4e4654a65d 100644
--- a/hw/xbox/nv2a/pgraph/vk/draw.c
+++ b/hw/xbox/nv2a/pgraph/vk/draw.c
@@ -1268,6 +1268,18 @@ static void begin_render_pass(PGRAPHState *pg)
         .clearValueCount = 0,
         .pClearValues = NULL,
     };
+    if (r->perf.timestamp_query_pool != VK_NULL_HANDLE) {
+        uint32_t query = r->perf.render_pass_query_pairs;
+        if (query < VK_PERF_RENDER_PASS_QUERY_PAIRS) {
+            vkCmdWriteTimestamp(r->command_buffer,
+                                VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT,
+                                r->perf.timestamp_query_pool,
+                                VK_PERF_RENDER_PASS_QUERY_BASE + 2 * query);
+            r->perf.render_pass_query_open = true;
+        } else {
+            r->perf.render_pass_query_overflows++;
+        }
+    }
     vkCmdBeginRenderPass(r->command_buffer, &render_pass_begin_info,
                          VK_SUBPASS_CONTENTS_INLINE);
     r->in_render_pass = true;
@@ -1278,6 +1290,15 @@ static void end_render_pass(PGRAPHVkState *r)
 {
     if (r->in_render_pass) {
         vkCmdEndRenderPass(r->command_buffer);
+        if (r->perf.render_pass_query_open) {
+            vkCmdWriteTimestamp(
+                r->command_buffer, VK_PIPELINE_STAGE_BOTTOM_OF_PIPE_BIT,
+                r->perf.timestamp_query_pool,
+                VK_PERF_RENDER_PASS_QUERY_BASE +
+                    2 * r->perf.render_pass_query_pairs + 1);
+            r->perf.render_pass_query_pairs++;
+            r->perf.render_pass_query_open = false;
+        }
         r->in_render_pass = false;
     }
 }
@@ -1304,6 +1325,10 @@ void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
     pgraph_vk_perf_record_finish_call(r, finish_reason);
 
     if (r->in_command_buffer) {
+        bool time_submit =
+            pgraph_vk_perf_should_time_finish(r, finish_reason);
+        bool gpu_time_submit =
+            r->perf.timestamp_query_pool != VK_NULL_HANDLE;
         uint64_t staged_bytes = 0;
         if (r->perf.enabled) {
             staged_bytes =
@@ -1321,14 +1346,29 @@ void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
         if (r->query_in_flight) {
             end_query(r);
         }
+        if (gpu_time_submit) {
+            vkCmdWriteTimestamp(r->command_buffer,
+                                VK_PIPELINE_STAGE_BOTTOM_OF_PIPE_BIT,
+                                r->perf.timestamp_query_pool, 3);
+        }
         VK_CHECK(vkEndCommandBuffer(r->command_buffer));
 
         VkCommandBuffer cmd = pgraph_vk_begin_single_time_commands(pg); // FIXME: Cleanup
+        if (gpu_time_submit) {
+            vkCmdResetQueryPool(cmd, r->perf.timestamp_query_pool, 0,
+                                VK_PERF_TIMESTAMP_QUERY_COUNT);
+            vkCmdWriteTimestamp(cmd, VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT,
+                                r->perf.timestamp_query_pool, 0);
+        }
         sync_staging_buffer(pg, cmd, BUFFER_INDEX_STAGING, BUFFER_INDEX);
         sync_staging_buffer(pg, cmd, BUFFER_VERTEX_INLINE_STAGING,
                                 BUFFER_VERTEX_INLINE);
         sync_staging_buffer(pg, cmd, BUFFER_UNIFORM_STAGING, BUFFER_UNIFORM);
         flush_memory_buffer(pg, cmd);
+        if (gpu_time_submit) {
+            vkCmdWriteTimestamp(cmd, VK_PIPELINE_STAGE_BOTTOM_OF_PIPE_BIT,
+                                r->perf.timestamp_query_pool, 1);
+        }
         VK_CHECK(vkEndCommandBuffer(r->aux_command_buffer));
         r->in_aux_command_buffer = false;
 
@@ -1353,8 +1393,6 @@ void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
         };
         nv2a_profile_inc_counter(NV2A_PROF_QUEUE_SUBMIT);
         vkResetFences(r->device, 1, &r->command_buffer_fence);
-        bool time_submit =
-            pgraph_vk_perf_should_time_finish(r, finish_reason);
         int64_t submit_start = time_submit ?
             qemu_clock_get_us(QEMU_CLOCK_REALTIME) : 0;
         VkResult result = vkQueueSubmit(
@@ -1387,9 +1425,91 @@ void pgraph_vk_finish(PGRAPHState *pg, FinishReason finish_reason)
         uint64_t wait_us = time_submit ?
             MAX(qemu_clock_get_us(QEMU_CLOCK_REALTIME) - wait_start, 0) : 0;
         VK_CHECK(result);
+        uint64_t gpu_batch_ns = 0;
+        uint64_t gpu_aux_ns = 0;
+        uint64_t gpu_handoff_ns = 0;
+        uint64_t gpu_main_ns = 0;
+        uint64_t gpu_vertex_upload_span_ns = 0;
+        uint64_t gpu_render_pass_span_ns = 0;
+        if (gpu_time_submit) {
+            uint64_t timestamps[4];
+            result = vkGetQueryPoolResults(
+                r->device, r->perf.timestamp_query_pool, 0,
+                ARRAY_SIZE(timestamps), sizeof(timestamps), timestamps,
+                sizeof(timestamps[0]), VK_QUERY_RESULT_64_BIT);
+            VK_CHECK(result);
+            uint64_t mask = UINT64_MAX;
+            if (r->perf.timestamp_valid_bits < 64) {
+                mask = (UINT64_C(1) << r->perf.timestamp_valid_bits) - 1;
+            }
+            double period = r->perf.timestamp_period_ns;
+            gpu_batch_ns = (uint64_t)
+                (((timestamps[3] - timestamps[0]) & mask) * period);
+            gpu_aux_ns = (uint64_t)
+                (((timestamps[1] - timestamps[0]) & mask) * period);
+            gpu_handoff_ns = (uint64_t)
+                (((timestamps[2] - timestamps[1]) & mask) * period);
+            gpu_main_ns = (uint64_t)
+                (((timestamps[3] - timestamps[2]) & mask) * period);
+            uint32_t pairs = r->perf.vertex_upload_query_pairs;
+            if (pairs > 0) {
+                uint64_t upload_timestamps[
+                    2 * VK_PERF_VERTEX_UPLOAD_QUERY_PAIRS];
+                result = vkGetQueryPoolResults(
+                    r->device, r->perf.timestamp_query_pool, 4,
+                    2 * pairs, sizeof(uint64_t) * 2 * pairs,
+                    upload_timestamps, sizeof(uint64_t),
+                    VK_QUERY_RESULT_64_BIT);
+                VK_CHECK(result);
+                for (uint32_t i = 0; i < pairs; i++) {
+                    gpu_vertex_upload_span_ns += (uint64_t)
+                        (((upload_timestamps[2 * i + 1] -
+                           upload_timestamps[2 * i]) & mask) * period);
+                }
+            }
+            assert(!r->perf.render_pass_query_open);
+            pairs = r->perf.render_pass_query_pairs;
+            if (pairs > 0) {
+                uint64_t pass_timestamps[
+                    2 * VK_PERF_RENDER_PASS_QUERY_PAIRS];
+                result = vkGetQueryPoolResults(
+                    r->device, r->perf.timestamp_query_pool,
+                    VK_PERF_RENDER_PASS_QUERY_BASE, 2 * pairs,
+                    sizeof(uint64_t) * 2 * pairs, pass_timestamps,
+                    sizeof(uint64_t), VK_QUERY_RESULT_64_BIT);
+                VK_CHECK(result);
+                for (uint32_t i = 0; i < pairs; i++) {
+                    gpu_render_pass_span_ns += (uint64_t)
+                        (((pass_timestamps[2 * i + 1] -
+                           pass_timestamps[2 * i]) & mask) * period);
+                }
+            }
+        }
         pgraph_vk_perf_record_finish_submit(
-            r, finish_reason, time_submit, submit_cpu_us, wait_us, staged_bytes,
+            r, finish_reason, time_submit, submit_cpu_us, wait_us,
+            gpu_time_submit, gpu_batch_ns, gpu_aux_ns, gpu_handoff_ns,
+            gpu_main_ns, staged_bytes,
             ARRAY_SIZE(submit_infos), 2);
+        if (gpu_time_submit) {
+            PGRAPHVkWaitStats *stats = &r->perf.finish[finish_reason];
+            stats->gpu_vertex_upload_span_ns += gpu_vertex_upload_span_ns;
+            stats->gpu_vertex_upload_timed_count +=
+                r->perf.vertex_upload_query_pairs;
+            stats->gpu_vertex_upload_active_pass_count +=
+                r->perf.vertex_upload_active_pass_pairs;
+            stats->gpu_vertex_upload_overflow_count +=
+                r->perf.vertex_upload_query_overflows;
+            stats->gpu_render_pass_span_ns += gpu_render_pass_span_ns;
+            stats->gpu_render_pass_timed_count +=
+                r->perf.render_pass_query_pairs;
+            stats->gpu_render_pass_overflow_count +=
+                r->perf.render_pass_query_overflows;
+        }
+        r->perf.vertex_upload_query_pairs = 0;
+        r->perf.vertex_upload_active_pass_pairs = 0;
+        r->perf.vertex_upload_query_overflows = 0;
+        r->perf.render_pass_query_pairs = 0;
+        r->perf.render_pass_query_overflows = 0;
         r->storage_buffers[BUFFER_VERTEX_RAM_STAGING].buffer_offset = 0;
         r->storage_buffers[BUFFER_TEXTURE_STAGING].buffer_offset = 0;
 
@@ -1433,6 +1553,11 @@ void pgraph_vk_begin_command_buffer(PGRAPHState *pg)
     };
     VK_CHECK(vkBeginCommandBuffer(r->command_buffer,
                                   &command_buffer_begin_info));
+    if (r->perf.timestamp_query_pool != VK_NULL_HANDLE) {
+        vkCmdWriteTimestamp(r->command_buffer,
+                            VK_PIPELINE_STAGE_TOP_OF_PIPE_BIT,
+                            r->perf.timestamp_query_pool, 2);
+    }
     pgraph_vk_invalidate_blend_constants(pg);
     r->command_buffer_start_time = pg->draw_time;
     r->in_command_buffer = true;
diff --git a/hw/xbox/nv2a/pgraph/vk/perf.c b/hw/xbox/nv2a/pgraph/vk/perf.c
index e847bc44a1..44db65227f 100644
--- a/hw/xbox/nv2a/pgraph/vk/perf.c
+++ b/hw/xbox/nv2a/pgraph/vk/perf.c
@@ -84,6 +84,34 @@ static void write_cpu_stat_array(FILE *file, const char *key,
     fputc(']', file);
 }
 
+static void init_gpu_timestamps(PGRAPHVkState *r)
+{
+    QueueFamilyIndices indices =
+        pgraph_vk_find_queue_families(r->physical_device);
+    uint32_t count = 0;
+    vkGetPhysicalDeviceQueueFamilyProperties(r->physical_device, &count, NULL);
+    g_autofree VkQueueFamilyProperties *props =
+        g_new(VkQueueFamilyProperties, count);
+    vkGetPhysicalDeviceQueueFamilyProperties(r->physical_device, &count, props);
+
+    assert(indices.queue_family >= 0);
+    assert((uint32_t)indices.queue_family < count);
+    r->perf.timestamp_valid_bits =
+        props[indices.queue_family].timestampValidBits;
+    r->perf.timestamp_period_ns = r->device_props.limits.timestampPeriod;
+    if (r->perf.timestamp_valid_bits == 0) {
+        return;
+    }
+
+    VkQueryPoolCreateInfo create_info = {
+        .sType = VK_STRUCTURE_TYPE_QUERY_POOL_CREATE_INFO,
+        .queryType = VK_QUERY_TYPE_TIMESTAMP,
+        .queryCount = VK_PERF_TIMESTAMP_QUERY_COUNT,
+    };
+    VK_CHECK(vkCreateQueryPool(r->device, &create_info, NULL,
+                               &r->perf.timestamp_query_pool));
+}
+
 void pgraph_vk_perf_init(PGRAPHVkState *r)
 {
     const char *path = g_getenv("XEMU_VK_PERF_LOG");
@@ -97,12 +125,22 @@ void pgraph_vk_perf_init(PGRAPHVkState *r)
         return;
     }
     r->perf.enabled = true;
+    init_gpu_timestamps(r);
     r->perf.last_flush_us = qemu_clock_get_us(QEMU_CLOCK_REALTIME);
     fprintf(r->perf.file,
-            "{\"type\":\"schema\",\"schema_version\":6"
+            "{\"type\":\"schema\",\"schema_version\":7"
             ",\"duration_sampling\":{\"initial_per_reason_per_frame\":%u"
-            ",\"hot_stride\":%u}",
-            VK_PERF_INITIAL_TIMED_SUBMITS, VK_PERF_HOT_SAMPLE_STRIDE);
+            ",\"hot_stride\":%u}"
+            ",\"gpu_batch_timestamps\":{\"supported\":%s"
+            ",\"timestamp_valid_bits\":%u"
+            ",\"timestamp_period_ns\":%.9g}"
+            ",\"vertex_upload_query_pairs_per_submit\":%u"
+            ",\"render_pass_query_pairs_per_submit\":%u",
+            VK_PERF_INITIAL_TIMED_SUBMITS, VK_PERF_HOT_SAMPLE_STRIDE,
+            r->perf.timestamp_query_pool != VK_NULL_HANDLE ? "true" : "false",
+            r->perf.timestamp_valid_bits, r->perf.timestamp_period_ns,
+            VK_PERF_VERTEX_UPLOAD_QUERY_PAIRS,
+            VK_PERF_RENDER_PASS_QUERY_PAIRS);
     write_names(r->perf.file, "finish_reasons", finish_reason_names,
                 ARRAY_SIZE(finish_reason_names));
     write_names(r->perf.file, "single_time_callers", single_time_reason_names,
@@ -114,6 +152,10 @@ void pgraph_vk_perf_init(PGRAPHVkState *r)
 
 void pgraph_vk_perf_finalize(PGRAPHVkState *r)
 {
+    if (r->perf.timestamp_query_pool != VK_NULL_HANDLE) {
+        vkDestroyQueryPool(r->device, r->perf.timestamp_query_pool, NULL);
+        r->perf.timestamp_query_pool = VK_NULL_HANDLE;
+    }
     if (r->perf.file != NULL) {
         fflush(r->perf.file);
         fclose(r->perf.file);
@@ -146,6 +188,11 @@ void pgraph_vk_perf_record_finish_submit(PGRAPHVkState *r,
                                          bool timed,
                                          uint64_t submit_cpu_us,
                                          uint64_t wait_us,
+                                         bool gpu_timed,
+                                         uint64_t gpu_batch_ns,
+                                         uint64_t gpu_aux_ns,
+                                         uint64_t gpu_handoff_ns,
+                                         uint64_t gpu_main_ns,
                                          uint64_t staged_bytes,
                                          uint64_t submit_info_count,
                                          uint64_t command_buffer_count)
@@ -162,6 +209,13 @@ void pgraph_vk_perf_record_finish_submit(PGRAPHVkState *r,
         stats->submit_cpu_us += submit_cpu_us;
         stats->wait_us += wait_us;
     }
+    if (gpu_timed) {
+        stats->gpu_timed_submit_count++;
+        stats->gpu_batch_ns += gpu_batch_ns;
+        stats->gpu_aux_ns += gpu_aux_ns;
+        stats->gpu_handoff_ns += gpu_handoff_ns;
+        stats->gpu_main_ns += gpu_main_ns;
+    }
     r->perf.submit_info_count += submit_info_count;
     r->perf.command_buffer_count += command_buffer_count;
     r->perf.staged_bytes += staged_bytes;
@@ -281,7 +335,7 @@ void pgraph_vk_perf_frame(PGRAPHVkState *r)
     int64_t now = qemu_clock_get_us(QEMU_CLOCK_REALTIME);
 
     fprintf(perf->file,
-            "{\"type\":\"frame\",\"schema_version\":6"
+            "{\"type\":\"frame\",\"schema_version\":7"
             ",\"timestamp_us\":%" PRId64 ",\"guest_frame\":%" PRIu64,
             now, ++perf->frame);
     write_stat_array(perf->file, "finish_count_per_guest_frame", perf->finish,
@@ -304,6 +358,53 @@ void pgraph_vk_perf_frame(PGRAPHVkState *r)
                      perf->finish,
                      ARRAY_SIZE(perf->finish),
                      offsetof(PGRAPHVkWaitStats, wait_us));
+    write_stat_array(perf->file,
+                     "finish_gpu_timed_submit_count_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_timed_submit_count));
+    write_stat_array(perf->file, "finish_gpu_batch_ns_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_batch_ns));
+    write_stat_array(perf->file, "finish_gpu_aux_ns_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_aux_ns));
+    write_stat_array(perf->file, "finish_gpu_handoff_ns_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_handoff_ns));
+    write_stat_array(perf->file, "finish_gpu_main_ns_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_main_ns));
+    write_stat_array(perf->file,
+                     "finish_gpu_vertex_upload_span_ns_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_vertex_upload_span_ns));
+    write_stat_array(perf->file,
+                     "finish_gpu_vertex_upload_timed_count_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_vertex_upload_timed_count));
+    write_stat_array(perf->file,
+                     "finish_gpu_vertex_upload_active_pass_count_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats,
+                              gpu_vertex_upload_active_pass_count));
+    write_stat_array(perf->file,
+                     "finish_gpu_vertex_upload_overflow_count_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats,
+                              gpu_vertex_upload_overflow_count));
+    write_stat_array(perf->file,
+                     "finish_gpu_render_pass_span_ns_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_render_pass_span_ns));
+    write_stat_array(perf->file,
+                     "finish_gpu_render_pass_timed_count_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats, gpu_render_pass_timed_count));
+    write_stat_array(perf->file,
+                     "finish_gpu_render_pass_overflow_count_per_guest_frame",
+                     perf->finish, ARRAY_SIZE(perf->finish),
+                     offsetof(PGRAPHVkWaitStats,
+                              gpu_render_pass_overflow_count));
     write_stat_array(perf->file, "single_time_submit_count_per_guest_frame",
                      perf->single_time, ARRAY_SIZE(perf->single_time),
                      offsetof(PGRAPHVkWaitStats, submit_count));
diff --git a/hw/xbox/nv2a/pgraph/vk/renderer.h b/hw/xbox/nv2a/pgraph/vk/renderer.h
index 5725c49e8d..c7d87a67de 100644
--- a/hw/xbox/nv2a/pgraph/vk/renderer.h
+++ b/hw/xbox/nv2a/pgraph/vk/renderer.h
@@ -398,8 +398,25 @@ typedef struct PGRAPHVkWaitStats {
     uint64_t submit_cpu_us;
     uint64_t wait_count;
     uint64_t wait_us;
+    uint64_t gpu_timed_submit_count;
+    uint64_t gpu_batch_ns;
+    uint64_t gpu_aux_ns;
+    uint64_t gpu_handoff_ns;
+    uint64_t gpu_main_ns;
+    uint64_t gpu_vertex_upload_span_ns;
+    uint64_t gpu_vertex_upload_timed_count;
+    uint64_t gpu_vertex_upload_active_pass_count;
+    uint64_t gpu_vertex_upload_overflow_count;
+    uint64_t gpu_render_pass_span_ns;
+    uint64_t gpu_render_pass_timed_count;
+    uint64_t gpu_render_pass_overflow_count;
 } PGRAPHVkWaitStats;
 
+#define VK_PERF_VERTEX_UPLOAD_QUERY_PAIRS 256
+#define VK_PERF_RENDER_PASS_QUERY_PAIRS 256
+#define VK_PERF_RENDER_PASS_QUERY_BASE (4 + 2 * VK_PERF_VERTEX_UPLOAD_QUERY_PAIRS)
+#define VK_PERF_TIMESTAMP_QUERY_COUNT (VK_PERF_RENDER_PASS_QUERY_BASE + 2 * VK_PERF_RENDER_PASS_QUERY_PAIRS)
+
 typedef enum PerfCpuRegion {
     VK_PERF_CPU_DRAW_BEGIN_SURFACE_UPDATE,
     VK_PERF_CPU_DRAW_FLUSH,
@@ -418,6 +435,15 @@ typedef struct PGRAPHVkCpuStats {
 typedef struct PGRAPHVkPerfTelemetry {
     FILE *file;
     bool enabled;
+    VkQueryPool timestamp_query_pool;
+    uint32_t timestamp_valid_bits;
+    float timestamp_period_ns;
+    uint32_t vertex_upload_query_pairs;
+    uint32_t vertex_upload_active_pass_pairs;
+    uint32_t vertex_upload_query_overflows;
+    uint32_t render_pass_query_pairs;
+    uint32_t render_pass_query_overflows;
+    bool render_pass_query_open;
     uint64_t frame;
     int64_t last_flush_us;
     PGRAPHVkWaitStats finish[VK_FINISH_REASON_COUNT];
@@ -670,6 +696,11 @@ void pgraph_vk_perf_record_finish_submit(PGRAPHVkState *r,
                                          bool timed,
                                          uint64_t submit_cpu_us,
                                          uint64_t wait_us,
+                                         bool gpu_timed,
+                                         uint64_t gpu_batch_ns,
+                                         uint64_t gpu_aux_ns,
+                                         uint64_t gpu_handoff_ns,
+                                         uint64_t gpu_main_ns,
                                          uint64_t staged_bytes,
                                          uint64_t submit_info_count,
                                          uint64_t command_buffer_count);
diff --git a/hw/xbox/nv2a/pgraph/vk/vertex.c b/hw/xbox/nv2a/pgraph/vk/vertex.c
index e53ddfb91c..912d1d8968 100644
--- a/hw/xbox/nv2a/pgraph/vk/vertex.c
+++ b/hw/xbox/nv2a/pgraph/vk/vertex.c
@@ -101,7 +101,24 @@ void pgraph_vk_update_vertex_ram_buffer(PGRAPHState *pg, hwaddr offset,
         return;
     }
 
+    uint32_t upload_query = r->perf.vertex_upload_query_pairs;
+    bool time_upload =
+        r->perf.timestamp_query_pool != VK_NULL_HANDLE &&
+        upload_query < VK_PERF_VERTEX_UPLOAD_QUERY_PAIRS;
+    bool had_active_pass = r->in_render_pass;
     VkCommandBuffer cmd = pgraph_vk_begin_nondraw_commands(pg);
+    if (time_upload) {
+        if (had_active_pass) {
+            r->perf.vertex_upload_active_pass_pairs++;
+        }
+        vkCmdWriteTimestamp(cmd,
+                            VK_PIPELINE_STAGE_BOTTOM_OF_PIPE_BIT,
+                            r->perf.timestamp_query_pool,
+                            4 + 2 * upload_query);
+        r->perf.vertex_upload_query_pairs++;
+    } else if (r->perf.timestamp_query_pool != VK_NULL_HANDLE) {
+        r->perf.vertex_upload_query_overflows++;
+    }
     void *copy_data[] = { data };
     VkDeviceSize copy_sizes[] = { size };
     VkDeviceSize staging_offset = pgraph_vk_append_to_buffer(
@@ -147,6 +164,11 @@ void pgraph_vk_update_vertex_ram_buffer(PGRAPHState *pg, hwaddr offset,
     vkCmdPipelineBarrier(cmd, VK_PIPELINE_STAGE_TRANSFER_BIT,
                          VK_PIPELINE_STAGE_VERTEX_INPUT_BIT, 0, 0, NULL, 1,
                          &after_copy, 0, NULL);
+    if (time_upload) {
+        vkCmdWriteTimestamp(cmd, VK_PIPELINE_STAGE_BOTTOM_OF_PIPE_BIT,
+                            r->perf.timestamp_query_pool,
+                            5 + 2 * upload_query);
+    }
     pgraph_vk_end_nondraw_commands(pg, cmd);
 
     nv2a_profile_inc_counter(NV2A_PROF_GEOM_BUFFER_UPDATE_1);
